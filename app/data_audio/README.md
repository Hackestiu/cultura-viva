# English benchmark audio

Here is the **synthetic** baseline set — reproducible, but not a substitute for
real speech and generated after executing `dataset_generator.py`. When present, `data_audio/recorded/` (see
[recorded/README.md](recorded/README.md)) is used instead automatically; this set
exists as the fallback and as the versioned baseline for regression comparisons.
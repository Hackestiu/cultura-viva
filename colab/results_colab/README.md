# Legacy Colab results

This directory contains benchmark outputs generated by the archived notebook in `../colab/`. It is historical reference data, not the output of the production application.

Current application runs write local CPU reports to `../app/results_computer/` and Arduino reports to `../app/results_arduino/`.